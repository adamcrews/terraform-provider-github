---
page_title: "github_agents_organization_secret_repositories (Resource) - GitHub"
subcategory: ""
description: |-
  Manages the repository allow list for a GitHub Agents organization secret.
---

# github_agents_organization_secret_repositories (Resource)

Manages the repository allow list for a GitHub Agents organization secret.

This resource manages the full set of repositories allowed to access an existing organization secret. You must have write access to an organization secret to use this resource.

This resource is only applicable when `visibility` of the existing organization secret has been set to `selected`.

## Example Usage

```terraform
resource "github_agents_organization_secret" "example" {
  secret_name = "EXAMPLE_SECRET_NAME"
  value       = "example-value"
  visibility  = "selected"
}

resource "github_repository" "example" {
  name       = "example-repo"
  visibility = "public"
}

resource "github_agents_organization_secret_repositories" "example" {
  secret_name             = github_agents_organization_secret.example.secret_name
  selected_repository_ids = [github_repository.example.repo_id]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `secret_name` (String) Name of the existing secret.
- `selected_repository_ids` (Set of Number) An array of repository ids that can access the organization secret.

### Read-Only

- `id` (String) The ID of this resource.

## Import

This resource can be imported using the secret name as the ID.

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = github_agents_organization_secret_repositories.example
  id = "secret-name"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import github_agents_organization_secret_repositories.example secret-name
```
